Build artifacts for Unlim8ted OS land here.

This directory is intentionally kept in the repository with only placeholder files. Target build scripts create and overwrite per-target output directories beneath it.
