#
# Raspberry Pi OS workaround for mkinitramfs "failed to determine device for /"
# during package hooks. Current Pi OS images can fail with MODULES=dep; use
# MODULES=most for provisioning stability.
#
MODULES=most
